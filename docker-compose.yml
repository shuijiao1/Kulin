name: kulin-nezha

services:
  dashboard:
    image: ghcr.io/shuijiao1/kulin:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kulin-nezha-dashboard
    restart: unless-stopped
    ports:
      - "8031:8008"
    volumes:
      - ./data:/dashboard/data
